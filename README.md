# digit_recognition_cnn
This project focuses on recognizing handwritten digits using deep learning. It employs a Convolutional Neural Network (CNN) trained on the MNIST dataset to accurately classify digits (0-9).
